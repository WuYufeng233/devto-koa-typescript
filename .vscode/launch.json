{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Node: Nodemon",
      // 选择进程或者指定默认端口号
      // "processId": "${command:PickProcess}",
      "port": 9229,
      "restart": true,
      "protocol": "inspector"
    }
  ]
}